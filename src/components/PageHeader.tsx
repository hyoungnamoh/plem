import { Text, View } from 'react-native';

const PageHeader = () => {
  return (
    <View style={{ flex: 1, backgroundColor: 'red', marginTop: 60 }}>
      <Text>하이 나는 페이지 헤더</Text>
    </View>
  );
};

export default PageHeader;
