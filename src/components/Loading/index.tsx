import { View } from 'react-native';
import PlemText from '../Atoms/PlemText';

const Loading = () => {
  return (
    <View>
      <PlemText>로딩중</PlemText>
    </View>
  );
};

export default Loading;
